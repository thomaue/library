//returns a 1, positive or not 
//depending on the number of - in str

int negativ_checker(char *str)
    {
        int i=0;
        int sign=1;
        while(str[i])
        {
            if(str[i]== '-')
                sign*=-1;
            i+=1;
        }
        return sign;
    }